[tool.poetry]
name = "utca"
version = "0.1.0"
description = "Universal token extractor"
authors = ["None"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
